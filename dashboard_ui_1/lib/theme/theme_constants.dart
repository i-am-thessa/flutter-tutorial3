import 'package:flutter/material.dart';

var myDefaultBackground = Colors.grey[300];

var kColorScheme =
    ColorScheme.fromSeed(seedColor: Color.fromARGB(255, 205, 215, 223));

var kDarkColorScheme = ColorScheme.fromSeed(
    brightness: Brightness.dark, seedColor: Color.fromARGB(255, 48, 192, 231));
